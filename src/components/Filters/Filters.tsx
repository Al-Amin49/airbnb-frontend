import React from 'react';

const Filters = () => {
    return (
        <div>
            <button>Filters</button>
            <button className='ml-2'>Dispaly total before taxes </button>
        </div>
    );
};

export default Filters;